apiVersion: v1 
kind: Secret 
metadata: 
  name: securecart-secret 
type: Opaque 

# Demonstration values only. Do not store real production secrets in Git.
stringData:
  DATABASE_USERNAME: demo-user
  DATABASE_PASSWORD: demo-password-not-for-production
  API_KEY: demo-api-key-not-for-production

  